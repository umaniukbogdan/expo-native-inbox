# expo-native-inbox
React Native (Expo) push notification demo with background task handling. Features: notification history tracking, deep linking, persistent storage via AsyncStorage. Supports foreground/background/terminated app states.
